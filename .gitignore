# Secrets & lokale Configs
config.ini
# config/config.json ist die Vorlage und bleibt im Repo
# Lokale Overrides (z.B. mit echter IP/Passwort) hier ausschließen:
config/config.local.json
*.log
*.db
*.opus
__pycache__/
*.pyc
recordings/
archive/
